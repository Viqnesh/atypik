{% extends 'base.html.twig' %}

{% block title %}Propriete{% endblock %}

{% block body %}
    <h1>Propriete</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ propriete.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ propriete.nom }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ propriete.type }}</td>
            </tr>
            <tr>
                <th>Obligatoire</th>
                <td>{{ propriete.obligatoire ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('propriete_index') }}">back to list</a>

    <a href="{{ path('propriete_edit', {'id': propriete.id}) }}">edit</a>

    {{ include('propriete/_delete_form.html.twig') }}
{% endblock %}
